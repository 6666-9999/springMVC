<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cssl.dao.UsersMapper">
	<select id="login" parameterType="users" resultType="users">
		select * from users where username=#{username} and password=#{password}
	</select>
	<insert id="register" parameterType="users">
		<selectKey keyProperty="usid" resultType="int" order="BEFORE">
			select users_seq.nextval from dual
		</selectKey>
		INSERT INTO USERS(usid,username,PASSWORD,name)
		VALUES(#{usid},#{username},#{password},#{name})
	</insert>
	<select id="isExists" parameterType="String" resultType="int">
		SELECT COUNT(1) FROM USERS WHERE username=#{name}
	</select>
</mapper>